services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: switchboard
      POSTGRES_PASSWORD: switchboard
      POSTGRES_DB: switchboard
    ports: ["5432:5432"]
    volumes: ["switchboard_pg:/var/lib/postgresql/data"]
volumes:
  switchboard_pg:
