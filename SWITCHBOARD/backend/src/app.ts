import express from 'express';import cors from 'cors';import helmet from 'helmet';import morgan from 'morgan';import {env} from './config/env.js';import {apiKey} from './middleware/auth.js';import {errors} from './middleware/error.js';import workflows from './routes/workflows.js';import runs from './routes/runs.js';import webhooks from './routes/webhooks.js';import directory from './routes/directory.js';import credentials from './routes/credentials.js';
export const app=express();app.use(helmet());app.use(cors({origin:env.FRONTEND_URL.split(',')}));app.use(express.json({limit:'1mb'}));app.use(morgan('dev'));app.get('/health',(_q,res)=>res.json({ok:true,service:'switchboard-api'}));app.use('/api',apiKey);app.use('/api/workflows',workflows);app.use('/api/runs',runs);app.use('/api/webhooks',webhooks);app.use('/api/directory',directory);app.use('/api/credentials',credentials);app.use(errors);
