import type{Request,Response}from'express';import*as service from'../services/trigger.service.js';import{triggerSchema}from'../validators/trigger.validator.js';import{audit}from'../services/audit.service.js';const p=(v:string|string[])=>Array.isArray(v)?v[0]:v;
export async function list(req:Request,res:Response){return res.json(await service.list(p(req.params.workflowId)))}
export async function create(req:Request,res:Response){const d=triggerSchema.parse(req.body);const t=await service.create(p(req.params.workflowId),d);await audit(req,'trigger.created','Trigger',t.id,{workflowId:t.workflowId,type:t.type});return res.status(201).json(t)}
export async function update(req:Request,res:Response){const d=triggerSchema.partial().parse(req.body);const t=await service.update(p(req.params.id),d);await audit(req,'trigger.updated','Trigger',t.id,{enabled:t.enabled,type:t.type});return res.json(t)}
export async function remove(req:Request,res:Response){const id=p(req.params.id);await service.remove(id);await audit(req,'trigger.deleted','Trigger',id);return res.status(204).end()}
export async function rotate(req:Request,res:Response){const t=await service.rotateSecret(p(req.params.id));await audit(req,'trigger.secret.rotated','Trigger',t.id);return res.json({id:t.id,secret:t.secret})}
