import type{Request,Response}from'express';import{prisma}from'../config/prisma.js';import{schedulerStatus}from'../services/scheduler.service.js';import{queueStatus}from'../services/queue.service.js';
export function live(_req:Request,res:Response){return res.json({ok:true,service:'switchboard-api',version:'2.1.0',uptime:Math.round(process.uptime()),timestamp:new Date().toISOString(),scheduler:schedulerStatus(),queue:queueStatus()})}
export async function ready(_req:Request,res:Response){try{await prisma.$queryRaw`SELECT 1`;const queued=await prisma.workflowRun.count({where:{status:'QUEUED'}});const running=await prisma.workflowRun.count({where:{status:'RUNNING'}});return res.json({ok:true,database:'connected',scheduler:schedulerStatus(),queue:{...queueStatus(),queued,running},timestamp:new Date().toISOString()})}catch{return res.status(503).json({ok:false,database:'unavailable',scheduler:schedulerStatus(),queue:queueStatus(),timestamp:new Date().toISOString()})}}
export async function metrics(_req:Request,res:Response){const since=new Date(Date.now()-24*60*60*1000);const runs=await prisma.workflowRun.findMany({where:{createdAt:{gte:since}},select:{status:true,createdAt:true,startedAt:true,finishedAt:true},orderBy:{createdAt:'desc'},take:1000});const finished=runs.filter(r=>r.startedAt&&r.finishedAt);const durations=finished.map(r=>r.finishedAt!.getTime()-r.startedAt!.getTime());const failed=runs.filter(r=>r.status==='FAILED').length;const succeeded=runs.filter(r=>r.status==='SUCCEEDED').length;const queued=runs.filter(r=>r.status==='QUEUED').length;const running=runs.filter(r=>r.status==='RUNNING').length;const avgDurationMs=durations.length?Math.round(durations.reduce((a,b)=>a+b,0)/durations.length):0;return res.json({window:'24h',runs:runs.length,succeeded,failed,queued,running,failureRate:runs.length?Number((failed/runs.length*100).toFixed(2)):0,averageDurationMs:avgDurationMs,queue:queueStatus(),scheduler:schedulerStatus(),timestamp:new Date().toISOString()})}
