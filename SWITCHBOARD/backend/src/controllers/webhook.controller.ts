import type{Request,Response}from'express';import{trigger}from'../services/webhook.service.js';const param=(v:string|string[])=>Array.isArray(v)?v[0]:v;export async function receive(req:Request,res:Response){const secret=req.header('x-switchboard-secret')||req.query.secret?.toString();const result=await trigger(param(req.params.slug),secret,req.body);if(result.kind==='not_found')return res.status(404).json({error:'Active webhook workflow not found'});if(result.kind==='unauthorized')return res.status(401).json({error:'Invalid webhook secret'});return res.status(202).json({runId:result.runId,status:result.status})}
