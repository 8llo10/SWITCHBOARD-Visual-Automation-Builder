import type{Request,Response}from'express';import*as service from'../services/credential.service.js';import{credentialSchema}from'../validators/credential.validator.js';
const param=(v:string|string[])=>Array.isArray(v)?v[0]:v;
export async function list(_q:Request,res:Response){return res.json(await service.list())}export async function create(req:Request,res:Response){const d=credentialSchema.parse(req.body);const c=await service.create(d);return res.status(201).json({id:c.id,name:c.name,type:c.type})}export async function update(req:Request,res:Response){const d=credentialSchema.parse(req.body);const c=await service.update(param(req.params.id),d);return res.json({id:c.id,name:c.name,type:c.type})}export async function remove(req:Request,res:Response){await service.remove(param(req.params.id));return res.status(204).end()}
