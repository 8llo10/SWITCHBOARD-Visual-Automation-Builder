import{Router}from'express';import{authorize}from'../middleware/auth.js';import*as controller from'../controllers/run.controller.js';const router=Router();router.get('/',controller.list);router.get('/:id',controller.get);router.post('/:id/approve',authorize('ADMIN','OPERATOR'),controller.approve);router.post('/:id/reject',authorize('ADMIN','OPERATOR'),controller.reject);router.post('/:id/cancel',authorize('ADMIN','OPERATOR'),controller.cancel);router.post('/:id/retry',authorize('ADMIN','OPERATOR'),controller.retry);export default router;
