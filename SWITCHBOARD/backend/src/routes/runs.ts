import{Router}from'express';import*as controller from'../controllers/run.controller.js';const router=Router();router.get('/',controller.list);router.get('/:id',controller.get);router.post('/:id/approve',controller.approve);router.post('/:id/reject',controller.reject);router.post('/:id/cancel',controller.cancel);router.post('/:id/retry',controller.retry);export default router;
