import{Router}from'express';import{authorize}from'../middleware/auth.js';import*as controller from'../controllers/workflow.controller.js';const router=Router();router.get('/',controller.list);router.post('/',authorize('ADMIN','OPERATOR'),controller.create);router.get('/:id',controller.get);router.put('/:id',authorize('ADMIN','OPERATOR'),controller.update);router.delete('/:id',authorize('ADMIN','OPERATOR'),controller.remove);router.post('/:id/run',authorize('ADMIN','OPERATOR'),controller.run);router.get('/:id/runs',controller.runs);export default router;
