import{Router}from'express';import{authorize}from'../middleware/auth.js';import*as c from'../controllers/trigger.controller.js';const r=Router();r.get('/workflow/:workflowId',c.list);r.post('/workflow/:workflowId',authorize('ADMIN','OPERATOR'),c.create);r.patch('/:id',authorize('ADMIN','OPERATOR'),c.update);r.delete('/:id',authorize('ADMIN','OPERATOR'),c.remove);r.post('/:id/rotate-secret',authorize('ADMIN'),c.rotate);export default r;
