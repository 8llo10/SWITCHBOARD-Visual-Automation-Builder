import{Router}from'express';import{authorize}from'../middleware/auth.js';import*as controller from'../controllers/credential.controller.js';const router=Router();router.get('/',authorize('ADMIN','OPERATOR'),controller.list);router.post('/',authorize('ADMIN'),controller.create);router.put('/:id',authorize('ADMIN'),controller.update);router.delete('/:id',authorize('ADMIN'),controller.remove);export default router;
