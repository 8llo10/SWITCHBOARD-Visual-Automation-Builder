"use client";
import {FormEvent,useState} from 'react';
import {useRouter} from 'next/navigation';
import {Workflow,LockKeyhole} from 'lucide-react';
import {sb,setSession} from '../../lib/switchboard';
export default function Login(){const router=useRouter();const[email,setEmail]=useState('admin@switchboard.local');const[password,setPassword]=useState('');const[error,setError]=useState('');const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{const r=await sb<any>('/auth/login',{method:'POST',body:JSON.stringify({email,password}),auth:false} as any);setSession(r.token,r.user);router.replace('/')}catch(e:any){setError(e.message)}finally{setBusy(false)}}return <main className="login-page"><form className="login-card" onSubmit={submit}><div className="login-logo"><Workflow/></div><span className="eyebrow">SWITCHBOARD</span><h1>Automation control plane</h1><p>Sign in to design, execute and audit IT workflows.</p><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<div className="login-error">{error}</div>}<button className="start-run" disabled={busy}><LockKeyhole size={16}/>{busy?'Signing in…':'Sign in'}</button></form></main>}
