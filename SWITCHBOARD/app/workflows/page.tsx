'use client';
import {FormEvent,useEffect,useState} from 'react';
import Link from 'next/link';
import {useRouter} from 'next/navigation';
import {Network,Plus,Workflow,ArrowRight,Play} from 'lucide-react';
import {Shell} from '../../components/Shell';
import {sb} from '../../lib/switchboard';
export default function WorkflowsPage(){const router=useRouter();const[rows,setRows]=useState<any[]>([]),[name,setName]=useState(''),[slug,setSlug]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');const load=()=>sb<any[]>('/workflows').then(setRows).catch(e=>setError(e.message));useEffect(()=>{void load()},[]);async function create(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{const created=await sb<any>('/workflows',{method:'POST',body:JSON.stringify({name,slug:slug||name.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,''),status:'DRAFT',definition:{nodes:[{id:'start',type:'custom',position:{x:180,y:260},data:{label:'Manual Trigger',kind:'trigger',config:{}}}],edges:[]}})});router.push(`/workflows/${created.id}`)}catch(e:any){setError(e.message)}finally{setBusy(false)}}return <Shell title="Workflows" eyebrow="AUTOMATION WORKSPACE"><div>{error&&<div className="error-panel">{error}</div>}<section className="neo-card"><div className="neo-section-head"><div><small>NEW AUTOMATION</small><h2>Create a workflow and start wiring nodes</h2></div><span className="muted">Create → add nodes → connect → run.</span></div><form className="neo-form" onSubmit={create}><input value={name} onChange={e=>setName(e.target.value)} placeholder="WORKFLOW NAME" required/><input value={slug} onChange={e=>setSlug(e.target.value)} placeholder="SLUG (OPTIONAL)"/><button className="primary-btn" disabled={busy}><Plus size={13}/>{busy?'CREATING':'CREATE & OPEN EDITOR'}</button></form></section><section className="neo-section"><div className="neo-section-head"><div><small>YOUR WORKFLOWS</small><h2>{rows.length} workflows</h2></div></div><div className="neo-workflow-grid">{rows.map(w=><Link href={`/workflows/${w.id}`} className="neo-workflow" key={w.id}><span className="node-mini"><Network size={16}/></span><h3>{w.name}</h3><p>Open the visual canvas to add nodes, connect steps and execute the workflow.</p><footer><span>{w.status}</span><span className="workflow-open">OPEN EDITOR <ArrowRight size={12}/></span></footer></Link>)}{!rows.length&&!error&&<div className="neo-card"><Workflow size={20}/><h3>No workflows yet</h3><p className="muted">Create one above and you will go directly into the node editor.</p></div>}</div></section></div></Shell>}
