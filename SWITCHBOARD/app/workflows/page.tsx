'use client';
import {FormEvent,useEffect,useState} from 'react';
import Link from 'next/link';
import {Network,Plus,Workflow} from 'lucide-react';
import {Shell} from '../../components/Shell';
import {sb} from '../../lib/switchboard';
export default function WorkflowsPage(){const[rows,setRows]=useState<any[]>([]),[name,setName]=useState(''),[slug,setSlug]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');const load=()=>sb<any[]>('/workflows').then(setRows).catch(e=>setError(e.message));useEffect(()=>{void load()},[]);async function create(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{await sb('/workflows',{method:'POST',body:JSON.stringify({name,slug:slug||name.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,''),status:'DRAFT',definition:{nodes:[{id:'start',type:'custom',position:{x:320,y:80},data:{label:'Manual Trigger',kind:'trigger',config:{}}}],edges:[]}})});setName('');setSlug('');await load()}catch(e:any){setError(e.message)}finally{setBusy(false)}}return <Shell title="Workflows" eyebrow="EVENT TOPOLOGY"><div>{error&&<div className="error-panel">{error}</div>}<section className="neo-card"><div className="neo-section-head"><div><small>CREATE DEFINITION</small><h2>New workflow</h2></div><span className="muted">Draft → wire services → publish.</span></div><form className="neo-form" onSubmit={create}><input value={name} onChange={e=>setName(e.target.value)} placeholder="WORKFLOW NAME" required/><input value={slug} onChange={e=>setSlug(e.target.value)} placeholder="EVENT-SLUG (OPTIONAL)"/><button className="primary-btn" disabled={busy}><Plus size={13}/>{busy?'CREATING':'CREATE'}</button></form></section><section className="neo-section"><div className="neo-section-head"><div><small>AUTOMATION NETWORK</small><h2>{rows.length} workflow definitions</h2></div></div><div className="neo-workflow-grid">{rows.map(w=><Link href={`/workflows/${w.id}`} className="neo-workflow" key={w.id}><span className="node-mini"><Network size={16}/></span><h3>{w.name}</h3><p>{w.description||'Publish, route and observe automation events.'}</p><footer><span>{w.status}</span><span>{w.slug} · V{w.version}</span></footer></Link>)}{!rows.length&&!error&&<div className="neo-card"><Workflow size={20}/><h3>No workflows yet</h3><p className="muted">Create the first event topology above.</p></div>}</div></section></div></Shell>}
