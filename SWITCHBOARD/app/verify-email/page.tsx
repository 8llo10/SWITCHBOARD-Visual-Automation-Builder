'use client';
import {useEffect,useState} from 'react';
import Link from 'next/link';
import {useRouter,useSearchParams} from 'next/navigation';
import {CheckCircle2,Mail,Network,RefreshCw,XCircle} from 'lucide-react';
import {sb} from '../../lib/switchboard';
export default function VerifyEmail(){const q=useSearchParams(),router=useRouter();const token=q.get('token'),email=q.get('email')||'';const[state,setState]=useState<'idle'|'working'|'verified'|'error'>(token?'working':'idle'),[message,setMessage]=useState(token?'Verifying secure link…':'Check your inbox for the verification link.'),[busy,setBusy]=useState(false);useEffect(()=>{if(!token)return;sb<any>('/auth/verify-email',{method:'POST',body:JSON.stringify({token}),auth:false} as any).then(()=>{setState('verified');setMessage('Email verified. Access is now enabled.');setTimeout(()=>router.replace('/login'),1400)}).catch((e:any)=>{setState('error');setMessage(e.message||'Verification link is invalid or expired.')})},[token,router]);async function resend(){if(!email)return;setBusy(true);try{const r=await sb<any>('/auth/resend-verification',{method:'POST',body:JSON.stringify({email}),auth:false} as any);setMessage(r.message||'Verification request accepted.')}catch(e:any){setMessage(e.message)}finally{setBusy(false)}}return <main className="terminal-auth"><div className="verify-wrap"><div className="terminal-mark"><Network size={18}/><span>SWITCHBOARD</span></div><div className={`verify-orb ${state}`} >{state==='verified'?<CheckCircle2 size={30}/>:state==='error'?<XCircle size={30}/>:<Mail size={30}/>}</div><small>IDENTITY VERIFICATION</small><h1>{state==='verified'?'Access granted.':state==='error'?'Verification failed.':'Verify your email.'}</h1><p>{message}</p>{email&&<code>{email}</code>}<div className="verify-actions">{state!=='verified'&&email&&<button className="terminal-submit" onClick={resend} disabled={busy}><RefreshCw size={13} className={busy?'spin':''}/>{busy?'SENDING…':'RESEND VERIFICATION'}</button>}<Link href="/login" className="ghost-btn">BACK TO SIGN IN</Link></div></div></main>}
