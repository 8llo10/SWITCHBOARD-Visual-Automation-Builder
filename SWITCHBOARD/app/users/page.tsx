'use client';
import {FormEvent,useEffect,useState} from 'react';
import {Plus,ShieldCheck,UserCog} from 'lucide-react';
import {Shell} from '../../components/Shell';
import {Badge,DataTable,formatDate} from '../../components/DataView';
import {sb} from '../../lib/switchboard';
export default function UsersPage(){const[rows,setRows]=useState<any[]>([]),[error,setError]=useState(''),[form,setForm]=useState({name:'',email:'',password:'',role:'OPERATOR'});const load=()=>sb<any[]>('/users').then(setRows).catch(e=>setError(e.message));useEffect(()=>{void load()},[]);async function create(e:FormEvent){e.preventDefault();try{await sb('/users',{method:'POST',body:JSON.stringify(form)});setForm({name:'',email:'',password:'',role:'OPERATOR'});await load()}catch(e:any){setError(e.message)}}async function toggle(r:any){try{await sb(`/users/${r.id}/status`,{method:'PATCH',body:JSON.stringify({active:!r.active})});await load()}catch(e:any){setError(e.message)}}return <Shell title="Platform users" eyebrow="ROLE-BASED ACCESS CONTROL"><div className="stack-lg">{error&&<div className="error-panel">{error}</div>}<div className="dashboard-grid"><section className="panel"><div className="panel-head"><div><p className="eyebrow">ACCESS MODEL</p><h2>Roles</h2></div><ShieldCheck size={20}/></div><div className="role-grid"><div><b>ADMIN</b><p>Platform-wide administration, credentials, users and audit.</p></div><div><b>OPERATOR</b><p>Operate owned workflows, runs, triggers and directory state.</p></div><div><b>VIEWER</b><p>Read-focused access to permitted operational data.</p></div></div></section><section className="panel"><div className="panel-head"><div><p className="eyebrow">CREATE ACCOUNT</p><h2>New user</h2></div><UserCog size={20}/></div><form className="stack-form" onSubmit={create}><label>Name<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/></label><label>Email<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/></label><label>Password<input type="password" minLength={10} value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/></label><label>Role<select value={form.role} onChange={e=>setForm({...form,role:e.target.value})}><option>ADMIN</option><option>OPERATOR</option><option>VIEWER</option></select></label><button className="primary-btn"><Plus size={15}/> Create user</button></form></section></div><section className="panel"><div className="panel-head"><div><p className="eyebrow">ACCOUNTS</p><h2>Users</h2></div></div><DataTable rows={rows} columns={[{key:'name',label:'Name'},{key:'email',label:'Email'},{key:'role',label:'Role',render:r=><Badge value={r.role}/>},{key:'active',label:'State',render:r=><Badge value={r.active?'Active':'Disabled'}/>},{key:'createdAt',label:'Created',render:r=>formatDate(r.createdAt)},{key:'id',label:'',render:r=><button className="table-action" onClick={()=>toggle(r)}>{r.active?'Disable':'Enable'}</button>}]}/></section></div></Shell>}
