'use client';
import {FormEvent,useState} from 'react';
import Link from 'next/link';
import {useRouter} from 'next/navigation';
import {ArrowLeft,Network,UserPlus} from 'lucide-react';
import {sb} from '../../lib/switchboard';
export default function Signup(){const router=useRouter();const[name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{const r=await sb<any>('/auth/register',{method:'POST',body:JSON.stringify({name,email,password}),auth:false} as any);if(r?.requiresVerification){router.replace(`/verify-email?email=${encodeURIComponent(email)}`)}else{router.replace('/login')}}catch(e:any){setError(e?.message||'Unable to create account')}finally{setBusy(false)}}return <main className="terminal-auth"><Link href="/" className="terminal-back"><ArrowLeft size={13}/> HOME</Link><div className="terminal-auth-wrap"><section className="terminal-auth-copy"><div className="terminal-mark"><Network size={18}/><span>SWITCHBOARD</span></div><small>NEW OPERATOR</small><h1>Join the<br/><em>control plane.</em></h1><p>Create an Operator account. Email verification is required before access is granted.</p><div className="terminal-lines"><span>01 · VERIFIED IDENTITY</span><span>02 · OPERATOR ROLE</span><span>03 · LIVE EXECUTION ACCESS</span></div></section><form className="terminal-form" onSubmit={submit}><header><span>AUTH / REGISTER</span><i/></header><label>NAME<input value={name} onChange={e=>setName(e.target.value)} minLength={2} required/></label><label>EMAIL<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>PASSWORD<input type="password" value={password} onChange={e=>setPassword(e.target.value)} minLength={10} required placeholder="AT LEAST 10 CHARACTERS"/></label>{error&&<div className="terminal-error">{error}</div>}<button className="terminal-submit" disabled={busy}><UserPlus size={13}/>{busy?'CREATING…':'CREATE OPERATOR'}</button><small>ALREADY REGISTERED? <Link href="/login">SIGN IN</Link></small></form></div></main>}
